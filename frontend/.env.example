# Base URL of the FastAPI backend, no trailing slash.
#
# Read at BUILD time and inlined into the client bundle. In Cloudflare Workers
# Builds, set it under Settings -> Build -> Variables and secrets so it is
# present when `next build` runs. Copy this file to .env.local for local dev.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
